on:
  schedule:
    - cron: "48 8 * * *"

jobs:
  get-issues:
    runs-on: ubuntu-latest
    steps:
    - run: |
        curl -s https://github.com/darensin01/goodfirstissues/releases/download/v1/golang_github > golang_github
        chmod +x golang_github
        ./golang_github
